# KarinFLiX Video Enhancement — Progress Tracker

> ⚠️ **Documento histórico.** Describe una sesión de diagnóstico anterior. El
> pipeline evolucionó: `FSRSuperEffect` y los upscalers DOG/Bicubic/Bilinear
> fueron retirados, y la cadena actual es: Depixel (debanding Weber-Fechner) →
> Detail Boost (RCAS/AMD) → Karin Light Boost (half-res, con Colors Boost
> fusionado) → MotionX2 Boost → Upsample bilinear (restauración de resolución).

## Success State (as of this session)

### Root cause finally found
The persistent "FSR shader compile error" (`ERROR: 0:35: 'max' : no matching overloaded function found` + `cannot convert from 'const mediump float' to 'mediump 3-component vector of float'`) was **NOT in FSRSuperEffect at all**.

**Root cause: `DetailBoostEffect.kt:109` used a 3-argument `max()`** which GLSL ES 2.0 does NOT support (only 2 args):

```glsl
vec3 maxRGB3x3 = max(max(nw, ne), max(sw, se), max(max(n, s), min(w, e)));  // BUG: 3-arg max + min(w,e) typo
```
Fixed to:
```glsl
vec3 maxRGB3x3 = max(max(max(nw, ne), max(sw, se)), max(max(n, s), max(w, e)));  // 2-arg max, max(w,e) correct
```

`DetailBoostEffect` sits EARLY in the pipeline (after LightBoost/FSR, before ColorBoost). When detail_boost was enabled, it crashed the whole video processing before `FSRSuperEffect` ever compiled. Every previous "FSR failed" test that had detail_boost ON was actually crashing at DetailBoost. This masked the true state of the FSR shaders.

### FSR consolidation now COMPLETE and WORKING
- `FSRSuperEffect.kt` = single combined FSR (EASU directional upscale + integrated RCAS sharpening + tone/chroma refinement) in ONE pass
- Uses `uTexelSize` uniform (NOT `textureSize` — this GLES 2.0 compiler rejects it)
- Uses only 2-argument `max()`/`min()`
- `drawFrame` sets `uSharpness` from `KEY_FSR_SHARPNESS` (default 60) plus `uTexelSize`
- **Verified A/B**: OFF vs FSR → ABS DIFF mean=0.768, max=58 → FSR genuinely sharpens (not passthrough); no GL errors

### Pipeline order (ExoPlayerActivity.kt)
`LowBitrateBoost → upscaler(MODE_FSR→FSRSuperEffect) → DetailBoost → LightBoost → ColorBoost`

### Lesson learned for future shader work
When a GLSL compile error appears at a CONSISTENT line number (0:35) across unrelated shader edits, suspect a DIFFERENT effect in the pipeline is crashing first, not the shader you're editing. Always check `logcat` for the actual `Program.<init>(SomeEffect.kt:NN)` class in the stack trace, not just the error message.

## Working features
- Detail Boost (fixed), Color Boost, Low Bitrate Boost, Light Boost, Demo mode
- Upscalers: Bilinear, Bicubic, DOG Sharpen — all working
- **FSR (unified 1.0+3.1+4.0)** — now working
- Debanding REMOVED by user request (`DebandEffect.kt` retained but unreferenced)
- Old broken FSR files (`FSREasuEffect.kt`, `FSRRcasEffect.kt`, `FSR31Effect.kt`, `FSR4Effect.kt`) kept unreferenced (per "don't delete without order")

## Note
- This file created to persist the DetailBoost root-cause discovery. Could be moved into the in-memory summary or deleted if not wanted.

---

# Sesión: KarinSuperResolucion (upscaler propio) — IMPLEMENTADO

> Estado al cierre: implementación completa y compilando (`:app:assembleDebug` OK,
> recompilación limpia de Kotlin sin errores). Pendiente **verificación en
> dispositivo** (la GLSL solo se compila en runtime).

## Pipeline actual de imagen (junto a este importa que hoy es:)
`Depixel → Detail Boost → Karin Light Boost → Colors Boost → Upscaler (KarinSuperRes/FSR/Anime4K) → MotionX2 Boost`. El upscaler FSR/Anime4K/Karin va como efecto libre después de Colors y antes de MotionX2.

## Qué se implementó (continuación de la sesión histórica)
- **ShaderBlobs.kt**: + `superresKarinEco` (~8 taps: bilineal + DoG con puerta
  por contraste + dering), `superresKarin` (~10 taps: gradiente 2x2 → dirección
  de borde, remate direccional en líneas, CAS de contraste adaptativo sobre el
  vecindario de salida, puerta de ruido + máscara de grano, DRS-aware, dering),
  `superresKarinEasu` (4 taps: 2x limpio, pase 1 de HiRes) y
  `karinSharpenFragment` (5 taps: pase 2 CAS/grano/DRS + demo split).
- **SuperResolutionEffect.kt**: `MODE_KARIN=3`, variantes `KARIN_ECO`/`KARIN_CRISP`,
  selección de fragment por variante y por 2 pases (`separateRcas`). Uniforms solo
  cuando existen (confirmado en `media3-common-1.9.3.aar`: `GlProgram.setFloatUniform`
  hace NPE si el uniform no está declarado en el shader escaneado).
- **SuperResRcasEffect.kt**: `casMode=true` (kernel KarinSharp) + `upscaleRatio`
  (lambda) para `uScaleFactor`.
- **ExoPlayerActivity.kt**: default `KEY_UPSCALER_MODE = MODE_KARIN`; por gama
  LOW→KARIN_ECO, MID→KARIN_CRISP, HIGH→2 pases (KarinEasu + KarinSharp); labels
  OSD "Karin / Karin ECO / Karin HiRes"; atenuación Detail Boost ×0.55; holder
  `karinScale[0] = outW/inW` (DRS-aware) alimentado por el callback GL y leído
  por el pase 2.
- **ExoPlayerSettingsHelper.kt**: radio "KarinSuperRes (Recomendado)" (default),
  modos válidos FSR/Anime4K/Karin.
- **VideoStatsHelper.kt**, **README.md** (:36, :50), **activity_tutorial.xml** (:204): textos actualizados.

## PENDIENTE — verificación en dispositivo (no ejecutable en este entorno)
1. **A/B con demo split** (activar Demo en ajustes) en las 3 gamas:
   - gama baja → confirmar Karin ECO (mitad derecha nítida, sin ruido amplificado ni halos).
   - gama media → confirmar Karin CRISP (texturas/pelo/vegetación nítidos, líneas de anime limpias).
   - gama alta → confirmar Karin HiRes (pase 2 de afilado real; mismo look que CRISP pero sin "borrosidad" en detalles finos).
2. **Comprobar logcat sin errores GL**: ningún `Program.<init>` con excepción
   `GlUtil$GlException` / `VideoFrameProcessingException` al activar cada variante.
3. **Prueba con DRS**: entrar a contenido 720p (re-escala 1.5x) y comparar que la
   fuerza de afilado baja vs. contenido 480p (2x) — visible si se sube la Nitidez.
4. **Fallback si un GLSL falla en runtime**: cambiar ese modo/variante a
   FRAGMENT_FSR (un solo pase) o FRAGMENT_FSR_EASU (2 pases) — ~15 líneas puntuales.
